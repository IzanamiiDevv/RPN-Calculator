type TDisplay = {
    display: string;
}

type TPosition = {
    x: number;
    y: number;
}

type TOffset = {
    x: number;
    y: number;
}

type Result = string | number;

export type { TDisplay, TPosition, TOffset, Result };